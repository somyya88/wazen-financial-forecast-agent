# Patch Notes V14.0.1 — Decision Indicators Runtime Fix

## سبب الإصلاح
ظهرت مشكلة تشغيل في صفحة مؤشرات القرار المالي بسبب استدعاء دالة `_basis_for_decision` داخل `app.py` دون تعريفها داخل الحزمة.

## ما تم إصلاحه
- إضافة دالة `_basis_for_decision` لتحديد معيار الحكم لكل بطاقة قرار.
- إضافة دالة `_decision_tone_from_status` لتحويل الحكم إلى لون/حالة واجهة.
- دعم معايير قطاعية داخلية عند وجودها من `sector_benchmarks.py`.
- دعم قواعد مالية عامة للسيولة والمديونية وهامش التشغيل عند عدم وجود معيار قطاعي.
- رفع رقم النسخة إلى V14.0.1.

## الاختبار
- `python -m py_compile *.py`
- `python tests_v13_4.py`
